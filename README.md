# Telegram-UserBot

My personal Telegram UserBot

env:

```bash
cp .env.example .env
YOUR_CODE_EDITOR .env
```

Generate the String Session:

```bash
uv run tools/string_session.py
```

run the UserBot:

```bash
uv run python3 -m tgubot
```
